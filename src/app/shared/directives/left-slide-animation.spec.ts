import { LeftSlideAnimation } from './left-slide-animation';

describe('LeftSlideAnimation', () => {
  it('should create an instance', () => {
    const directive = new LeftSlideAnimation();
    expect(directive).toBeTruthy();
  });
});

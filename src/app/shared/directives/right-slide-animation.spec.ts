import { RightSlideAnimation } from './right-slide-animation';

describe('RightSlideAnimation', () => {
  it('should create an instance', () => {
    const directive = new RightSlideAnimation();
    expect(directive).toBeTruthy();
  });
});
